const person = {
  name: "Goodness",
  age: 12,
  county: "St. saviour",
};

function logData() {
  const sentence =
    person.name + " is " + age + " years old and lives in " + person.county;
}

logData();
